/**
 * @swagger
 * components:
 *   securitySchemes:
 *     Bearer:
 *       type: apiKey
 *       name: Authorization
 *       in: header
 *       description: >- 
 *        Enter the token from the `/login` endpoint with the `Bearer` prefix, e.g. "Bearer abcde12345".
 */